const API_PREFIX = '/api';

const GET_TEXT_BY_HASH = API_PREFIX + '/users/get-text/:hash';

const GET_USERS_LIST_WITH_PROGRESS = API_PREFIX + '/admin/get-users';

module.exports = {
    GET_TEXT_BY_HASH,
    GET_USERS_LIST_WITH_PROGRESS
};
